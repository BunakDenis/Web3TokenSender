# README — Інструкція користувача для Web3 Token Sender

## Опис
Web3 Token Sender — це простий веб-інтерфейс для відправки ETH через RPC-провайдера з використанням приватного ключа безпосередньо у браузері. Проєкт призначений **виключно для навчання та тестових мереж** (у прикладах використовується Sepolia).

⚠️ **Увага:** не використовуйте приватні ключі від реальних гаманців або кошти з mainnet.

---

## Швидкий старт

1. Відкрийте файл `index.html` у браузері.
2. Заповніть усі необхідні поля форми.
3. Натисніть кнопку **Send**.
4. Повідомлення про результат з’явиться у спливаючому вікні, а детальні логи — у консолі браузера.

---

## Опис полів форми

### Private key
Приватний ключ гаманця, з якого буде відправлено ETH.  
Формат: hex-рядок (`0x...` або без `0x`).

### Transfer amount (ETH)
Сума ETH для відправки.  
Приклад: `0.01`

### Public key (recipient)
Ethereum-адреса отримувача у форматі `0x...`.

### RPC Provider
RPC-сервер для підключення до мережі:
- **Infura (Sepolia)** — потребує API-ключ.
- **Alchemy (Sepolia)** — потребує API-ключ.
- **Public Sepolia RPC** — ключ не потрібен.
- **Custom RPC URL** — власний RPC-сервер.

### RPC Provider key
API-ключ для Infura або Alchemy.  
Є іконка 👁 для показу / приховування значення.

### Custom RPC URL
Показується лише при виборі `Custom RPC URL`.  
Потрібно вказати повний URL RPC-сервера.

---

## Логіка роботи з RPC

- **Infura / Alchemy**  
  Фінальний RPC URL формується так:
  ```text
  <base_url><API_KEY>
